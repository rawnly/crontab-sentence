{
  "name": "crontab-sentence",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm run build --filter docs",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^2.8.4",
    "turbo": "latest"
  }
}
